// Конфигурация аренды (единственное место для изменений)
const rentalConfig = {
  rentedUntil: '', // или '' если свободно
};

// Доступные форматы даты (для валидации)
const dateFormats = {
  display: 'дд-мм-гггг',
  storage: 'DD-MM-YYYY',
};
