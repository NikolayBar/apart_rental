// Конфигурация аренды (единственное место для изменений)
const rentalConfig = {
  rentedUntil: '10-10-2025', // или '' если свободно
};

// Доступные форматы даты (для валидации)
const dateFormats = {
  display: 'дд-мм-гггг',
  storage: 'DD-MM-YYYY',
};
